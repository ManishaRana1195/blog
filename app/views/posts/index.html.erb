<% @posts.each do |post| %>
	<div class="post_wrapper">
		<h3 class="title"><%= link_to post.title, post %></h3>
		<p class="date">Created at <%= post.created_at.strftime('%B, %d, %Y')%></p>
	</div>
<% end %>

<%= link_to "Add A Post", new_post_path, class: "btn btn-primary"%>